# Repo Conventions

- Rust crates use `geoclt-*` naming.
- Python package path is `python/geoclt`.
- Schema files under `schemas/` are canonical contract artifacts.
- Sidecar protocol definitions live under `proto/`.
