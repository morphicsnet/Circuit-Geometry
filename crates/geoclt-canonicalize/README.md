# geoclt-canonicalize

`geoclt-canonicalize` turns verified mechanism outputs into stable mechanism identities, classes, and stability metrics.

It is a supported internal algorithm crate in the Python-first Geo-CLT release train.
