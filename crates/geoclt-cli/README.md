# geoclt-cli

`geoclt-cli` is the operator-facing command-line surface for Geo-CLT benchmark, artifact validation, and sidecar workflows.

It is an operational internal crate in the Python-first Geo-CLT release train.
