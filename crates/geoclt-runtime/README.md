# geoclt-runtime

`geoclt-runtime` is the staged runtime spine that turns atlas, metric, transport, hypergraph, and causal outputs into typed artifact bundles.

It is a supported internal algorithm crate in the Python-first Geo-CLT release train.
