# geoclt-artifacts

`geoclt-artifacts` assembles, canonicalizes, validates, and compatibility-checks typed Geo-CLT artifact bundles.

This crate is part of the Python-first Geo-CLT release train. It is intended to be packageable and externally supportable, but not marketed as a standalone end-user product yet.
