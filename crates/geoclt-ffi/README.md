# geoclt-ffi

`geoclt-ffi` is the internal PyO3 bridge that exposes Geo-CLT kernels and runtime bundle entrypoints to Python.

It is an operational internal crate in the Python-first Geo-CLT release train and is not intended as a standalone user-facing Rust API.
