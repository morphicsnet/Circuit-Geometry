# geoclt-atlas

`geoclt-atlas` provides deterministic atlas fitting and chart-overlap diagnostics for the Geo-CLT staged runtime.

It is a supported internal algorithm crate in the Python-first Geo-CLT release train.
