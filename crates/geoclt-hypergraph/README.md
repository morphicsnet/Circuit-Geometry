# geoclt-hypergraph

`geoclt-hypergraph` builds deterministic candidate events and admitted hyperpaths from upstream Geo-CLT runtime inputs.

It is a supported internal algorithm crate in the Python-first Geo-CLT release train.
